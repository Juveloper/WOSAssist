<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Netzwerk-Zugriff -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Medienzugriff (je nach Android-Version unterschiedlich) -->

    <!-- Android 12 und darunter -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Android 14+ (optional, nur wenn du gezielt selektierte Bilder brauchst) -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Kamera nur optional -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WOSAssist"
        tools:targetApi="34">

        <!-- MainActivity (Startbildschirm) -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.WOSAssist"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.WOSAssist" />

        <!-- Auftrag erstellen -->
        <activity
            android:name=".CreateItemActivity"
            android:exported="true"
            android:theme="@style/Theme.WOSAssist"/>

        <!-- Aufträge anzeigen -->
        <activity
            android:name=".ViewItemsActivity"
            android:exported="true"
            android:theme="@style/Theme.WOSAssist"/>

        <!-- Detailansicht -->
        <activity
            android:name=".DetailActivity"
            android:exported="true"
            android:theme="@style/Theme.WOSAssist"/>

        <!-- Chat -->
        <activity
            android:name=".ChatActivity"
            android:exported="true"
            android:theme="@style/Theme.WOSAssist"/>

    </application>
</manifest>